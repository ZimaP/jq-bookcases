{
  "name": "jq-bookcases",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --check site.js && node --check configurator-3d.js && node --check bookcase-config.js && node --check bookcase-layout.js && node --check bookcase-bom.js && node --check bookcase-pricing.js && node --check bookcase-engine.js && node --check bookcase-render-contract.js && node --check benjamin-moore-colors.js",
    "test": "node --test tests/*.test.js",
    "test:browser": "playwright test",
    "serve": "python3 -m http.server 5173"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
